# Monitoring Foundation

Phase 1 ships scrape targets and dashboard ownership boundaries. API metrics, worker metrics, queue latency, provider health, payment failures, moderation volume, and audit write failures should be promoted to first-class metrics as each module moves from mock to live providers.
