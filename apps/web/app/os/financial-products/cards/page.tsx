"use client";

import { useCallback, useEffect, useState } from "react";
import { CreditCard, X } from "lucide-react";
import { motion } from "framer-motion";

import { Badge, Button, Panel, PermissionDenied } from "@fliptrybe/ui";

import { EmptyState, ErrorNotice, LoadingBlock } from "../../../campaigns/components";
import { isForbiddenError } from "../../../lib/api-client";
import { enrollCardCustomer, formatNaira, freezeCard, fundCard, issueCard, loadCardCostPreview, loadCardEnrollment, loadCards, terminateCard, unfreezeCard, withdrawFromCard, type CardCostPreview, type CardCurrency, type CardEnrollment, type VirtualCard, type VirtualCardStatus } from "../api";

const CARD_STATUS_TONE: Record<VirtualCardStatus, "success" | "warning" | "danger"> = { ACTIVE: "success", FROZEN: "warning", TERMINATED: "danger" };
const CARD_CURRENCIES: CardCurrency[] = ["USD", "NGN"];

export default function CardsTabPage() {
  const [cards, setCards] = useState<VirtualCard[]>([]); const [cardsLoading, setCardsLoading] = useState(true); const [cardholderName, setCardholderName] = useState(""); const [fundingNaira, setFundingNaira] = useState(5000); const [issuingCard, setIssuingCard] = useState(false); const [cardActionId, setCardActionId] = useState<string>(); const [error, setError] = useState<string>(); const [forbidden, setForbidden] = useState(false); const [cardAmountInput, setCardAmountInput] = useState<Record<string, string>>({}); const [currency, setCurrency] = useState<CardCurrency>("USD"); const [enrollment, setEnrollment] = useState<CardEnrollment>(); const [enrollmentLoading, setEnrollmentLoading] = useState(false); const [costPreview, setCostPreview] = useState<CardCostPreview | null>(null); const [enrolling, setEnrolling] = useState(false); const [enrollForm, setEnrollForm] = useState({ firstName:"",lastName:"",email:"",phone:"",dateOfBirth:"",street:"",city:"",state:"",country:"NG",postalCode:"",idType:"BVN",idNumber:"",idImageBase64:"" });
  const refreshCards=useCallback(async()=>{setError(undefined);setForbidden(false);try{setCards(await loadCards())}catch(caught){setError(caught instanceof Error?caught.message:"We could not load your cards.");setForbidden(isForbiddenError(caught))}finally{setCardsLoading(false)}},[]); useEffect(()=>{void refreshCards()},[refreshCards]);
  const refreshEnrollment=useCallback(async(forCurrency:CardCurrency)=>{setEnrollmentLoading(true);try{setEnrollment(await loadCardEnrollment(forCurrency))}catch{setEnrollment(undefined)}finally{setEnrollmentLoading(false)}},[]); useEffect(()=>{void refreshEnrollment(currency)},[currency,refreshEnrollment]);
  useEffect(()=>{if(currency==="NGN"||fundingNaira<=0){setCostPreview(null);return}let cancelled=false;void loadCardCostPreview(currency,Math.round(fundingNaira*100)).then(preview=>{if(!cancelled)setCostPreview(preview)}).catch(()=>{if(!cancelled)setCostPreview(null)});return()=>{cancelled=true}},[currency,fundingNaira]);
  const submitIssueCard=useCallback(async()=>{setIssuingCard(true);setError(undefined);try{const result=await issueCard(cardholderName.trim(),fundingNaira*100,currency);if(result.status!=="active")setError("Card funding was charged but issuance is still being confirmed — check back shortly.");setCardholderName("");await refreshCards()}catch(caught){setError(caught instanceof Error?caught.message:"Could not issue this card.")}finally{setIssuingCard(false)}},[cardholderName,currency,fundingNaira,refreshCards]);
  const submitEnrollment=useCallback(async()=>{setEnrolling(true);setError(undefined);try{await enrollCardCustomer({firstName:enrollForm.firstName.trim(),lastName:enrollForm.lastName.trim(),email:enrollForm.email.trim(),phone:enrollForm.phone.trim(),currency,country:enrollForm.country.trim()||"NG",...(enrollForm.dateOfBirth?{dateOfBirth:enrollForm.dateOfBirth}:{}),...(enrollForm.street.trim()?{address:{street:enrollForm.street.trim(),city:enrollForm.city.trim(),state:enrollForm.state.trim(),country:enrollForm.country.trim()||"NG",...(enrollForm.postalCode.trim()?{postalCode:enrollForm.postalCode.trim()}: {})}}:{}),...(enrollForm.idType?{idType:enrollForm.idType}:{}),...(enrollForm.idNumber.trim()?{idNumber:enrollForm.idNumber.trim()}:{}),...(enrollForm.idImageBase64.trim()?{idImageBase64:enrollForm.idImageBase64.trim()}: {})});await refreshEnrollment(currency)}catch(caught){setError(caught instanceof Error?caught.message:"Could not complete verification.")}finally{setEnrolling(false)}},[currency,enrollForm,refreshEnrollment]);
  const needsEnrollment=Boolean(enrollment?.required&&!enrollment.enrolled);
  const submitFundCard=useCallback(async(card:VirtualCard)=>{const major=Number(cardAmountInput[card.id]??"");if(!Number.isFinite(major)||major<=0){setError("Enter an amount to top up.");return}setCardActionId(card.id);setError(undefined);try{await fundCard(card.id,Math.round(major*100));setCardAmountInput(prev=>({...prev,[card.id]:""}));await refreshCards()}catch(caught){setError(caught instanceof Error?caught.message:"Could not fund this card.")}finally{setCardActionId(undefined)}},[cardAmountInput,refreshCards]);
  const submitWithdraw=useCallback(async(card:VirtualCard)=>{const major=Number(cardAmountInput[card.id]??"");if(!Number.isFinite(major)||major<=0){setError("Enter an amount to withdraw back to your wallet.");return}setCardActionId(card.id);setError(undefined);try{await withdrawFromCard(card.id,Math.round(major*100));setCardAmountInput(prev=>({...prev,[card.id]:""}));await refreshCards()}catch(caught){setError(caught instanceof Error?caught.message:"Could not withdraw from this card.")}finally{setCardActionId(undefined)}},[cardAmountInput,refreshCards]);
  const submitToggleFreeze=useCallback(async(card:VirtualCard)=>{setCardActionId(card.id);setError(undefined);try{if(card.status==="FROZEN")await unfreezeCard(card.id);else await freezeCard(card.id);await refreshCards()}catch(caught){setError(caught instanceof Error?caught.message:"Could not update this card.")}finally{setCardActionId(undefined)}},[refreshCards]);
  const submitTerminateCard=useCallback(async(id:string)=>{setCardActionId(id);setError(undefined);try{await terminateCard(id);await refreshCards()}catch(caught){setError(caught instanceof Error?caught.message:"Could not terminate this card.")}finally{setCardActionId(undefined)}},[refreshCards]);
  if(forbidden)return <PermissionDenied>You do not have permission to view virtual cards for this workspace. Contact your workspace owner if you believe this is a mistake.</PermissionDenied>;
  return <motion.div animate={{opacity:1}} className="mt-6" initial={{opacity:0}}><ErrorNotice message={error}/><Panel className="overflow-hidden p-0"><div className="border-b border-[var(--ft-border)] bg-[var(--ft-bg-muted)] p-5 sm:p-6"><div className="flex items-start gap-4"><div className="grid size-11 place-items-center rounded-2xl bg-[var(--ft-accent)]/10 text-[var(--ft-accent)]"><CreditCard className="size-5"/></div><div><div className="font-mono text-[9px] uppercase tracking-[0.16em] text-[var(--ft-accent)]">Global spending</div><h1 className="mt-1 text-xl font-semibold">Virtual cards for international spending</h1><p className="mt-1 max-w-2xl text-sm leading-6 text-[var(--ft-text-muted)]">Use supported card products for international online services and subscriptions. Choose the card currency, review the indicative cost, then issue and manage your card.</p></div></div><div className="mt-4 flex flex-wrap gap-2 text-[10px] font-mono uppercase tracking-wider"><span className="rounded-full border border-[var(--ft-border)] px-2 py-1">Subscriptions</span><span className="rounded-full border border-[var(--ft-border)] px-2 py-1">International checkout</span><span className="rounded-full border border-[var(--ft-border)] px-2 py-1">Freeze / unfreeze</span></div></div><div className="p-5"><label className="mb-1 block text-xs text-[var(--ft-text-muted)]">Card currency</label><div className="flex gap-2">{CARD_CURRENCIES.map(code=><button className={["h-10 flex-1 rounded-[var(--radius-lg)] border text-sm font-medium transition-colors",currency===code?"border-[var(--ft-accent)] bg-[var(--ft-accent)]/10 text-[var(--ft-accent)]":"border-[var(--ft-border)] bg-[var(--ft-bg-surface)] text-[var(--ft-text-secondary)]"].join(" ")} key={code} onClick={()=>setCurrency(code)} type="button">{code}</button>)}</div>{enrollmentLoading?<p className="mt-4 text-xs text-[var(--ft-text-muted)]">Checking requirements…</p>:needsEnrollment?<div className="mt-4"><div className="rounded-[var(--radius-md)] border border-[var(--ft-border)] bg-[var(--ft-bg-muted)] p-3 text-xs text-[var(--ft-text-secondary)]">A {currency} card is issued by {enrollment?.providerName}, which requires a verified identity before your first card.</div><div className="mt-3 grid gap-3 sm:grid-cols-2">{([['firstName','First name','Jane'],['lastName','Last name','Doe'],['email','Email','jane@example.com'],['phone','Phone (with country code)','+2348030000000'],['dateOfBirth','Date of birth (YYYY-MM-DD)','1995-04-12'],['street','Street address','12 Admiralty Way'],['city','City','Lekki'],['state','State','Lagos'],['country','Country code','NG'],['postalCode','Postal code','100001'],['idNumber','ID number','22200000000'],['idImageBase64','ID image URL or base64','https://...']] as const).map(([field,label,placeholder])=><label className="block" key={field}><span className="mb-1 block text-xs text-[var(--ft-text-muted)]">{label}</span><input className="h-11 w-full rounded-[var(--radius-lg)] border border-[var(--ft-border)] bg-[var(--ft-bg-surface)] px-3 text-sm outline-none placeholder:text-[var(--ft-text-muted)] focus:border-[var(--ft-accent)]" onChange={e=>setEnrollForm(prev=>({...prev,[field]:e.target.value}))} placeholder={placeholder} value={enrollForm[field]}/></label>)}<label className="block"><span className="mb-1 block text-xs text-[var(--ft-text-muted)]">ID type</span><select className="h-11 w-full rounded-[var(--radius-lg)] border border-[var(--ft-border)] bg-[var(--ft-bg-surface)] px-3 text-sm outline-none focus:border-[var(--ft-accent)]" onChange={e=>setEnrollForm(prev=>({...prev,idType:e.target.value}))} value={enrollForm.idType}>{["BVN","NIN","PASSPORT","VIN"].map(t=><option key={t} value={t}>{t}</option>)}</select></label></div><Button className="mt-4 w-full justify-center" disabled={enrolling||!enrollForm.firstName.trim()||!enrollForm.lastName.trim()||!enrollForm.email.trim()||!enrollForm.phone.trim()} onClick={()=>void submitEnrollment()}>{enrolling?"Verifying…":`Verify identity for ${currency} cards`}</Button></div>:<><label className="mb-1 mt-4 block text-xs text-[var(--ft-text-muted)]">Cardholder name</label><input className="h-12 w-full rounded-[var(--radius-lg)] border border-[var(--ft-border)] bg-[var(--ft-bg-surface)] px-4 text-sm outline-none placeholder:text-[var(--ft-text-muted)] focus:border-[var(--ft-accent)]" onChange={e=>setCardholderName(e.target.value)} placeholder="e.g. Jane Doe" value={cardholderName}/><label className="mb-1 mt-4 block text-xs text-[var(--ft-text-muted)]">Initial funding ({currency})</label><input className="h-12 w-full rounded-[var(--radius-lg)] border border-[var(--ft-border)] bg-[var(--ft-bg-surface)] px-4 text-sm outline-none focus:border-[var(--ft-accent)]" min={100} onChange={e=>setFundingNaira(Number(e.target.value))} type="number" value={fundingNaira}/>{costPreview&&<p className="mt-2 text-xs text-[var(--ft-text-muted)]">Costs about {formatNaira(costPreview.walletCostMinor)} at ₦{costPreview.rate.toFixed(2)}/{costPreview.cardCurrency} — indicative.</p>}<Button className="mt-4 w-full justify-center" disabled={!cardholderName.trim()||fundingNaira<100||issuingCard} onClick={()=>void submitIssueCard()}>{issuingCard?"Issuing…":`Issue ${currency} card`}</Button></>}</div></Panel><div className="mt-4">{cardsLoading?<Panel className="p-6"><LoadingBlock label="Loading your cards"/></Panel>:cards.length===0?<Panel className="p-6"><EmptyState copy="Issue a supported card above to manage it here." icon={CreditCard} title="No cards yet"/></Panel>:<div className="grid gap-3">{cards.map(card=><Panel className="p-4" key={card.id}><div className="flex items-center gap-4"><div className="grid size-10 place-items-center rounded-full bg-[var(--ft-accent)]/10"><CreditCard className="size-4 text-[var(--ft-accent)]"/></div><div className="min-w-0 flex-1"><div className="font-semibold">{card.cardholderName}</div><div className="mt-0.5 text-xs text-[var(--ft-text-muted)]">{card.currency} · •••• {card.last4}</div></div><Badge tone={CARD_STATUS_TONE[card.status]}>{card.status.toLowerCase()}</Badge></div><div className="mt-4 grid gap-2 sm:grid-cols-[1fr_auto_auto]"><input className="h-10 rounded-lg border border-[var(--ft-border)] bg-[var(--ft-bg-surface)] px-3 text-sm" onChange={e=>setCardAmountInput(prev=>({...prev,[card.id]:e.target.value}))} placeholder="Amount" value={cardAmountInput[card.id]??""}/><Button disabled={cardActionId===card.id} onClick={()=>void submitFundCard(card)} variant="secondary">Fund</Button><Button disabled={cardActionId===card.id} onClick={()=>void submitWithdraw(card)} variant="secondary">Withdraw</Button></div><div className="mt-2 flex justify-end gap-2"><Button className="h-9 px-3 text-xs" disabled={cardActionId===card.id||card.status==="TERMINATED"} onClick={()=>void submitToggleFreeze(card)} variant="secondary">{card.status==="FROZEN"?"Unfreeze":"Freeze"}</Button><Button className="h-9 px-3 text-xs" disabled={cardActionId===card.id||card.status==="TERMINATED"} onClick={()=>void submitTerminateCard(card.id)} variant="secondary"><X className="size-3"/>Terminate</Button></div></Panel>)}</div>}</div></motion.div>;
}
